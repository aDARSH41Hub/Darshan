/**
 * chat service — Milestone M12.
 *
 * Socket.IO chat — mutual-accept only, block/report ships same release
 *
 * Not implemented yet — stub exists so the repo structure and import
 * paths are correct from day one.
 */

// TODO (M12): implement chat service
module.exports = {};
